# Janus review — Stage 5 (tables)

**Date:** 2026-07-20  
**Reviewer:** Janus (third source)  
**Code:** `778d275` feat tables · merge `be90c73` on `main`  
**Owner:** `[claude-code]`  
**Verdict:** **ACCEPT** after independent gates (unit + LO PDF visual).  
**Note:** Prior HANDOFF claim of “done” before Janus visual pass was **process-invalid**. This file is the formal accept.

---

## Gates (definition used)

| Gate | Required for `[x]` on tables? | Result |
|------|-------------------------------|--------|
| `npm test` including `tables.test.js` | Yes | **Pass** (8 table tests + full suite) |
| Merged to `main` (not only feature branch) | Yes | **`be90c73`** |
| XML: real `w:tbl`, DXA widths, CLEAR shade not SOLID | Yes | **Pass** |
| LibreOffice PDF render of fixtures | Yes (tables) | **Pass** — PDFs produced + text checked |
| Janus written review | Yes (per Marcus 2026-07-20) | **This file** |

Tests alone would have been **insufficient** for Stage 5.

---

## Evidence

### Unit / XML
- `ShadingType.CLEAR` → `w:val="clear"`, fill `D9D9D9` (3 header cells on shaded fixture); **no** `solid`.
- `tblW` 9000 DXA; `gridCol` 3000×3 sum 9000; `tcW` type dxa on cells.
- Merged fixture: `vMerge` present (2), `gridSpan` val 3.
- Nested `<strong>` in cell covered by unit test.

### Visual (Janus-run, not owner self-report only)
```
node bin/cli.js fixtures/table_shaded_header.html -o out/stage5-review/shaded.docx
node bin/cli.js fixtures/table_merged_cells.html -o out/stage5-review/merged.docx
python3 scripts/office/soffice.py … → shaded.pdf, merged.pdf
```
PDF text (pypdf):
- **shaded:** `Name Role Team` / `Ada Engineer Platform` / `Grace Lead Engineer Compilers`
- **merged:** `Quarter Revenue Notes` / `Q1 100 Jan` / `120 Feb` / `Totals below`  
  — **Q1 appears once** (rowspan not duplicating label); colspan footer text present.

Artifacts: `out/stage5-review/` (local; not required in git).

---

## What went well

1. Real `Table` / `TableRow` / `TableCell` — not fake paragraph grids.
2. Header detection: `thead` **or** `th` → bold + CLEAR shade.
3. HTML table layout walk for rowspan column occupancy; trusts docx to emit vMerge CONTINUE.
4. Explicit DXA on table + cells + equal column split with remainder on last col.
5. Explicit grid borders (avoids invisible default).
6. Deviations write-up in BUILD_PLAN is substantive and accurate.
7. Registered only via `BLOCK_HANDLERS.table` — low blast radius on lists.

---

## Issues (non-blocking)

### P2 — Single paragraph per cell
Multiple `<p>` inside `<td>` collapse to one para via inline walk. Documented out of scope. Stage 8 if manuals need multi-para cells.

### P2 — Fixed 9000 DXA / equal columns only
No `colgroup`, no CSS width, no `%` from HTML. Fine for v1; brand templates may want `--reference` + width hints later.

### P2 — `querySelectorAll('tr')` flattens nested tables
A `<table>` inside a cell would pull inner rows into the outer walk. Rare; Stage 8: only direct section rows or stop at nested table.

### P3 — Owner “visually verified” in HANDOFF before Janus
Process smell (same class as false “main clean”). Fixed going forward by **Definition of done** in HANDOFF/BUILD_PLAN/JANUS.md.

### P3 — Floating dirty file on review checkout
`test/lists.test.js` had uncommitted `testPostListElementsHaveNoNumbering` (good test; already passing when present). **Commit or drop** — do not leave truth only in the working tree.

---

## Scope compliance

| Plan item | Met? |
|-----------|------|
| Real docx Table | Yes |
| Header bold + CLEAR shade | Yes |
| columnWidths + cell width DXA | Yes |
| colspan / rowspan | Yes |
| Fixtures shaded + merged | Yes |
| Wire blocks.js | Yes |

---

## Sign-off

**Stage 5 accepted** on `main` @ `be90c73` (code `778d275`).  

Safe to start Stage 6 (images) and/or Stage 7 (CLI) **only with HANDOFF Git truth matching `git status` / `git log -1`**. Do not mark future stages `[x]` until unit **and** (for layout-sensitive stages) LO visual **and** Janus review when Marcus requires third-source accept.
