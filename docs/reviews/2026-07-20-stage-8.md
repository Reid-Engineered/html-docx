# Janus review — Stage 8 (polish / edge-case hardening)

**Date:** 2026-07-20  
**Reviewer:** Janus  
**Commits:** `fe4de4e` feat · `f5b2e1c` docs mark complete  
**Owner:** `[antigravity]`  
**Verdict:** **ACCEPT**

---

## Gates

| Gate | Result |
|------|--------|
| On `main` | Yes (`f5b2e1c`) |
| `npm test` (+ `test/polish.test.js` ×5) | **Pass** (59 tests) |
| Plan: empty/malformed, nested spans, CSS shorthands, `--theme` | **Met** |
| Nested list numbering P1 (Stage 4 debt) | **Addressed** (nested `ol` → decimal, not letters) |
| LO PDF `fixtures/polish_full.html` | **Pass** — headings, lists, shorthand/malformed text visible |
| Deviations in BUILD_PLAN | Present (claims “None” then describes work — minor honesty) |

---

## Evidence (Janus-run)

```text
npm test → all green incl. polish suite
node bin/cli.js fixtures/polish_full.html -o out/stage8-review/polish.docx --toc
  → Verify PDF OK
node bin/cli.js … --theme dark --no-verify → polish-dark.docx
```

**Nested list probe** (default nested `ol`):
- Outer `ilvl=0` / inner `ilvl=1` separate `numId` (architecture unchanged)
- Inner abstract levels are **`decimal` / `%1.` `%2.` …** — no longer forced `lowerLetter` at L1  
- Explicit `type="a"` / `type="i"` / `list-style-type` still map (tests assert `lowerLetter`, `lowerRoman`, square bullet)

**PDF text:** “Polish and Edge…”, “Decimal level 0”, “Lower alpha”, “Default nested decimal”, “shorthand”, “Unclosed” all present.

**Theme:** `theme: 'dark'` → Segoe UI + FFFFFF / E0E0E0 in document XML.

**Shorthand:** `font: … Arial` → Arial + bold in output; full italic bit not strongly asserted in PDF path (unit test covers `parseFontShorthand`).

---

## What went well

1. Closed long-standing **nested list P1** with default-decimal levels + `list-style-type` / HTML `type` map.  
2. **`--theme`** (modern/classic/dark/creative) wired CLI → `convert` → cascade.  
3. **CSS `font` / `background` shorthand** expansion in `style.js`.  
4. Malformed/empty HTML doesn’t crash; deep spans keep color+bold.  
5. `polish_full.html` + focused unit tests; LO path used for layout fixture.

---

## Residual issues (non-blocking)

| Sev | Issue |
|-----|--------|
| P2 | Nested lists still allocate a **new** numbering instance + elevated `ilvl` (not one Word abstract with multi-level counters). Markers are correct *format*; multi-level `%1.%2` chaining across parent/child instances is still limited. |
| P2 | BUILD_PLAN “Deviations: None” while adding themes, shorthand, list rewrite — should say “expanded beyond bullets”. |
| P2 | HANDOFF snapshot HEAD still pointed at `fe4de4e` while tip is `f5b2e1c` (process drift lite). |
| P3 | `list-style-type` on `<li>` (fixture square) vs on `<ul>`/`<ol>` — confirm cascade always puts it on the list node `computedStyle` for nested cases. |
| P3 | Image P2s (relative paths, 4:3) untouched — fine for Stage 8 scope. |

---

## Scope compliance

| Plan item | Met? |
|-----------|------|
| empty elements | Yes |
| malformed / unclosed tags | Yes (tolerate) |
| deeply nested spans | Yes |
| CSS shorthands (`font`, `background`) | Yes |
| `--theme` override | Yes |
| Nested list numbering fix (debt) | Yes (format default) |

---

## Project status after Stage 8

**Stages 0–8 complete on `main`.** Janus accepts on record for **3–8** after this file.  

Optional follow-ons (not stage-gated): push origin, prune old `stage-*` branches, image relative paths, true single-abstract nested numbering, `verify.sh` double-LO cleanup.

---

## Sign-off

**Stage 8 ACCEPT.** Polish goals met; nested-list letter bug fixed; theme + shorthand land cleanly.
