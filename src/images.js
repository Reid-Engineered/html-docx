/**
 * Converts HTML images to docx ImageRuns.
 * @param {import('node-html-parser').HTMLElement} node
 * @param {Object} options
 * @returns {Array<any>}
 */
export function convertImage(node, options = {}) {
  return [];
}
