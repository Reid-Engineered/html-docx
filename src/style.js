/**
 * Resolves computed styles for DOM nodes.
 * @param {import('node-html-parser').HTMLElement} node
 * @param {Array<Object>} styleStack
 * @returns {Object}
 */
export function getComputedStyle(node, styleStack = []) {
  return {};
}
