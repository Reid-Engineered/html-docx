/**
 * Converts a block-level HTML element to docx block components.
 * @param {import('node-html-parser').HTMLElement} node
 * @param {Object} options
 * @returns {Array<any>}
 */
export function convertBlock(node, options = {}) {
  return [];
}
